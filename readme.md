# Linux-Copilot 🐧
A natural language AI assistant for Linux.

## Setup
1. Clone the repo: `git clone https://github.com/norbreaks/Linux-Copilot.git`
2. Install dependencies: `pip install -r requirements.txt`
3. Run: `python main.py`